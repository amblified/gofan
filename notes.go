package main

func _() {

	// alternative
	// (1) x := read 50
	// findBiggestRuleThatIsBelow(x)
	// log Mode
	// apply level
	// while nextValue := f(); nextValue > transition_when_below {} // should also check if temp exceeds bigger rule and then apply that rule
	// timeout?
	// redo from (1)

}
